
namespace FileManager.Command
{
    internal enum UserCommand
    {
        Unknown,
        ChangeCurrentDirectory,
        CreateDirectory,
        CreateFile,
        WriteToFile,
        Remove,
        Move,
        Rename
    }
}
